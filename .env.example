# LLM provider: "claude-agent-sdk" (local dev, subscription auth) | "openrouter" (deployed)
LLM_PROVIDER=claude-agent-sdk

# Only needed when LLM_PROVIDER=openrouter
OPENROUTER_API_KEY=
OPENROUTER_MODEL=deepseek/deepseek-chat

# Exa web search
EXA_API_KEY=
